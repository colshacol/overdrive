import React from "react";
import ReactDOM from "react-dom";

import styled from "styled-components";

import * as styles from "../styles/brand";

export const Container = styled.div`
  ${styles.flexColumn}
  ${styles.rounded}
`;
